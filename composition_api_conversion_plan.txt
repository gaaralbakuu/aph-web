Plan to update remaining files to Composition API script setup (Vue 2.7.16):

Directory: src/views/sysbasics/video/adminViews/component/lesson
1. Catalogue.vue
2. Content.vue
3. Course.vue
4. CourseCopy.vue
5. Tag.vue
6. Topic.vue

Directory: src/views/sysbasics/video/adminViews/component/system
1. Banner.vue
2. College.vue
3. Menu.vue
4. Role.vue
5. Setting.vue

Conversion Steps for each file:
1.  Use `<script setup>`.
2.  Import `ref`, `reactive`, `computed`, `onMounted`, `getCurrentInstance` from 'vue'.
3.  Import `useLocalI18n` from '@/composables/useLocalI18n'.
4.  Initialize i18n: `const { l, c } = useLocalI18n('componentName')`.
5.  Access global properties via `const { proxy } = getCurrentInstance()`.
6.  Convert `data()` to `reactive` state or `ref`.
7.  Convert `methods` to standard functions.
8.  Convert `computed` properties.
9.  Convert lifecycle hooks (`mounted` -> `onMounted`, etc.).
10. Update template to use `l` instead of `$l` and remove `this` references.
11. Verify imports of child components.
