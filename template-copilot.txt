- Chuyển đổi áp dụng cho file: 

* thực hiện chuyển đổi các tag của el- sang antdesign
- các a-drawer sử dụng thì dùng :visible thay vì v-model:open và @close="showObj.xxx = false"
- không chuyển đổi el-tree
- mấy cái value="" thì đừng dùng :value=""


* chuyển đổi API options thành composition API (script setup) cho tôi, thực hiện sử dụng tanstack query
- không dùng được defineOptions
- api thì dùng 
import api from '@/api'
- đa ngôn ngữ thì dùng
import { useLocalI18n } from '@/composables/useLocalI18n'
- store thì dùng 
import store from '@/store'
dùng thêm các cái này nếu cần
const instance = getCurrentInstance()
const route = instance.proxy.$route
const router = instance.proxy.$router
const { $request } = instance.proxy
const { l, c } = useLocalI18n('videoUserPlay')

refactor code:
z-table (là el-table trong component z-table đã được import) -> a-table (của antd 1.7) phù hợp cho vue 2.7.16
Không cần phải import vì đã Vue.use trong main rồi
Không cài thêm package vì nó đã được cài trước đó